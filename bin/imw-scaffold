#!/usr/bin/env ruby
$: << File.join(File.dirname(__FILE__), '../lib/')
require 'imw/utils'
require 'imw/workflow/scaffold'
# require 'fileutils'; include FileUtils::DryRun
include IMW

scaffold_dataset ARGV if ARGV.length >= 1
